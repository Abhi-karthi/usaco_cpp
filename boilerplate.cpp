#include <iostream>
using namespace std;

void solve(int n) {
    cout << n << "\n";
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    // freopen("problemname.in", "r", stdin);
    // freopen("problemname.out", "w", stdout);

    int n;
    cin >> n;
    solve(n);

    return 0;
}